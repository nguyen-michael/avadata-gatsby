import React from 'react'
import Layout from '../components/Layout'

const NotFoundPage = () => (
  <Layout>
    <div>
      <p>Sorry, that link doesn't exist. Return to Home Page</p>
    </div>
  </Layout>
)

export default NotFoundPage
